# GATES — psychic-plugins

| Gate | Scope | Evidence | Status |
|---|---|---|---|
| PLG-0 | Birth: plugin manifest + self-marketplace, three prose-first skills (request-contract, gate-machine, unknown-audit), validator mechanizing the platform's own frontmatter constraints, claude-plugin-validate acceptance | Born under the parent psychic-crew HELIX gate SIDE-2; closure rides the same operator token, stamped in both ledgers | **APPROVED** `APPROVE SIDE-2` @ 2026-08-26T20:46:27Z |
| S0-RECONCILE |  | MIT LICENSE + canonical identity (Nathan Lim) + explainer-epoch port (visibility lines deferred to S1 — still true today) | multi-repo gate (one token, per-repo stamps after per-repo push verification); append-only history law held | **APPROVED** `APPROVE S0-RECONCILE` @ 2026-08-31T23:39:46Z |
| S1-PLUGINS-WEB |  | the web-only rebuild per the operator rulings: manifests + slash-install lanes retired from HEAD (history preserved); gate-machine reborn as pure ledger discipline; README = the chat-user gallery; sibling .gitignore + the stage-everything publication probe ported (pre-flip hygiene); zero-CLI-content arm over every shipped skill body (executable shapes, fragment-assembled, fire-probed); the last interpreter dependency eliminated with the manifests it parsed | revert unit: this whole gate; pin: S1B (the flip). The flip is NOT here — S1B is its own terminal gate with the history scan and the operator ruling on history disclosure | **APPROVED** `APPROVE S1-PLUGINS-WEB` @ 2026-08-31T23:57:24Z |
