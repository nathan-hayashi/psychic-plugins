# S5-README-UX (plugins leg), explained plainly

## What changed

A bound skills badge, the three-surfaces Mermaid diagram (one prose-first file → chat upload,
Claude Code, or pasted raw — with the no-sync platform law stated), a "What is not asserted"
section, and the skill-count binding rewritten first-occurrence → every-occurrence with a
planted-conflict control.

## Why

The gallery README is this repo's storefront; its numbers must be as bound as its skills are
CLI-free. The diagram states the shipping model in one glance instead of three paragraphs.

## Verify it yourself

```
./scripts/validate-plugins.sh | tail -4
```

## What could break, and what catches it

Badge/prose drift → named FAIL. The diagram is prose-only content — the zero-CLI scan does not
apply to README surfaces and never did; the scanner's scope statement in the Verification
section is unchanged.
