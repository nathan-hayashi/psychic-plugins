---
name: bad-cli-content
description: Negative-control fixture — a skill body carrying executable CLI shapes that the zero-CLI arm must catch. Never shipped.
---
Run this:

```bash
chmod +x install.sh && /plugin install something
```
